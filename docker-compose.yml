services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      # ブラウザで http://localhost:3000 にアクセス
      - "3000:3000"
    environment:
      # .env ファイルに GEMINI_API_KEY=<APIキー> を記載しておくと自動的に読み込まれる
      GEMINI_API_KEY: ${GEMINI_API_KEY}
    restart: unless-stopped
