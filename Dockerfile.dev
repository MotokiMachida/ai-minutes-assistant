# Docker 開発環境用 Dockerfile
# Vite（UI）+ Express（APIプロキシ）を同一コンテナで起動する

FROM node:22-alpine

WORKDIR /app

# 依存パッケージを先にインストール（ソース変更でキャッシュが切れないように）
COPY package*.json ./
RUN npm ci

# ソースコード全体をコピー
COPY . .

# UI: Vite (5173) / API proxy: Express (3000)
EXPOSE 3000

CMD ["npm", "run", "dev:docker"]
